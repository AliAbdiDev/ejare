export type MainParams = { home: string }

export type User = {
    mobile: string,
    isActive: true,
    lastLogin: number,
    loginHistory: [],
    id: string,
    createdAt: number,
    updatedAt: number
}